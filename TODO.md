CLI argument for web generation :
  - generate pages for each pass
  - then generate the navigation

Anyway:
  - auto update keplers if older than 7 days ?

CLI argument for checking directories if needed
